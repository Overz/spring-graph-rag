/**
 * Identidade que atravessa os módulos ({@code CallerContext}, RF30/ADL-008) — interface
 * nomeada do módulo {@code shared}.
 */
@NamedInterface("security")
package com.github.overz.shared.security;

import org.springframework.modulith.NamedInterface;
