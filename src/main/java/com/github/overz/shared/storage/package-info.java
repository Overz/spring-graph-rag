/**
 * Porta de armazenamento de artefatos de documento (ADR-001) — interface nomeada do
 * módulo {@code shared}. A implementação vive em {@code shared.internal}.
 */
@NamedInterface("storage")
package com.github.overz.shared.storage;

import org.springframework.modulith.NamedInterface;
