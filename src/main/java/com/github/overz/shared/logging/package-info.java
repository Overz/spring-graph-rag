/**
 * Logging centralizado do projeto ({@code ILogger}/{@code LoggerFactory}) — interface
 * nomeada do módulo {@code shared}. Nenhuma classe chama SLF4J direto.
 */
@NamedInterface("logging")
package com.github.overz.shared.logging;

import org.springframework.modulith.NamedInterface;
