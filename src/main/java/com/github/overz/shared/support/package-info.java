/**
 * Utilitários binários simples reutilizáveis entre módulos — interface nomeada do
 * módulo {@code shared}.
 */
@NamedInterface("support")
package com.github.overz.shared.support;

import org.springframework.modulith.NamedInterface;
